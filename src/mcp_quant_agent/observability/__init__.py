"""Observability — Langfuse Cloud tracing for every agent decision.

Use ``from mcp_quant_agent.observability.langfuse_setup import get_langfuse_handler``
to obtain the LangGraph callback handler.
"""
