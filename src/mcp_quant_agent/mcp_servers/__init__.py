"""MCP server modules for data, analytics, and execution.

Each sub-package exports a FastMCP application that can be run standalone
(``python -m mcp_quant_agent.mcp_servers.data.server``) or used in-process
during backtests with the shared module-level clock.
"""
